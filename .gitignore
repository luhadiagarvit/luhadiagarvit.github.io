# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# misc
*.pem
.cache
.astro

# v3 — local-only assets (embeddings index, full-resolution photos, derived data)
private/
public/_index/
public/_data/
# public/portrait/ is generated by `pnpm portrait:render --force` but
# committed so CI/GitHub Pages has the files at deploy time.

# wrangler local state (KV cache, dev sockets, etc.)
.wrangler/